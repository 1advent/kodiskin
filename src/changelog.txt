[v1.2.2]
- Fixed Gear integration calls to use the correct addon id (`plugin.video.gears`), restoring home widgets and search results loading.

[v1.2.1]
- Removed Gear addon gating from the home widget includes so the widgets always render.

[v1.2.0]
- Renamed the visible search provider slot from FEN Light to Gear so the search UI matches the new addon backend.

[v1.1.9]
- Bumped the skin version after switching the skin's search and widget routing over to Gear.

[v1.1.8]
- Switched the skin's FENlight/FEN integration over to the Gear addon for search and widget navigation.

[v1.1.7]
- Fixed helper RunScript argument parsing so `mode` and `query` are parsed correctly from Kodi comma-separated script arguments.

[v1.1.6]
- Restored original-style recent-search execution flow so selecting a history item updates the search strings first and forces the results container to refresh.
- Stopped reloading the full skin during re-search operations.

[v1.1.5]
- Fixed Recent searches re-run behavior by passing the parsed clicked query directly into the helper re_search handler.

[v1.1.4]
- Fixed Recent searches click handling to re-run the selected stored search term instead of falling back to the new-search prompt.
- Improved helper RunScript argument parsing for Kodi's comma-separated invocation format.

[v1.1.3]
- Fixed Recent searches clicks to run searches directly from the stored history term.
- Increased the default visible Recent searches count from 5 to 10.

[v1.1.2]
- Switched Recent searches list rendering to Skin.String(SearchHistory.N) slots for compatibility with runtime helper behavior.
- Added query-argument re-search handling so selecting a history item always re-runs that exact term.

[v1.1.1]
- Bumped skin version to keep release metadata aligned with the latest helper/repository updates.

[v1.1.0]
- Fixed search history list: item labels were invisible due to undefined 'darkgrey' colour; changed to 'unfocused_text'.

[v1.0.9]
- Escaped generated search-history XML labels so special characters do not break the history list.
- Updated search-history publishing reliability in the helper add-on.

[v1.0.8]
- Fixed search history labels to render actual past search terms.
- Restored re-search behavior for visible history entries.

[v1.0.7]
- Restored dynamic recent-search history list behavior on the search results screen.
- Removed redundant manual left-panel search actions so only real prior searches appear.

[v1.0.6]
- Fixed search screen focus so top-left search controls are reachable.
- Added empty-state search actions when recent search history is unavailable.

[v1.0.5]
- Refreshed non-home background handling to match the green home background.
- Republished fanart assets so Kodi detail pages and repository views pick up the updated artwork.

[v1.0.2]
- Updated packaged install assets and repository integration.
- Refreshed install icon/logo assets.
- Bundled repository/helper update path improvements.

[v1.0.1]
- Initial public release of Fentastic Oneadvent Skin.
- Unique add-on ID to avoid conflicts with the original FENtastic skin.
- Simplified home menu for Movies, TV Shows, Add-ons, and Favorites.
- FenLight/FenLightAM search and direct section routing.
- Oneadvent branding and green home background.
